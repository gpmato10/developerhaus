<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="developerhaus.domain.User">

	<!-- 단건 조회 -->
    <select id="findMemberById" parameterType="java.lang.String" resultType="developerhaus.domain.User">
		SELECT
		 	SEQ
		   ,ID
		   ,NAME
		   ,PASSWORD
		FROM
			USERS
		WHERE
			ID = #{value}
    </select>
    
</mapper>
